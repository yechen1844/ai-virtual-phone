# Roche APK 打包指南

## 一、这是什么？

这是一套**一键打包工具**，用于把 Roche 网页版打包成 Android APK。

打包后的 APK 包含以下增强功能：
- **沉浸式全屏**（无状态栏、无导航栏）
- **原生后台保活**（前台 Service + 静音音频循环，无需网络）
- **蓝牙 BLE 支持**（控制外设玩具）
- **麦克风权限**
- **推送通知**

---

## 二、环境要求

打包前，你的电脑必须安装以下软件：

### 1. Node.js 18+
- 下载地址：https://nodejs.org/
- 验证：打开 PowerShell 输入 `node -v`

### 2. JDK 21+（必须带 jlink）
- 推荐 Eclipse Temurin 21：https://adoptium.net/temurin/releases/?version=21
- 验证：`java -version`
- 脚本会自动搜索系统中的 JDK，优先选择带 jlink 的版本
- **重要**：必须 JDK 21+，JDK 17 不行（capacitor-filesystem 8.1.2 要求 JDK 21）
- 如果只装了 JDK 17，脚本会尝试通过 foojay-resolver 自动下载 JDK 21，但建议直接安装 JDK 21

### 3. Android SDK
- 安装 Android Studio：https://developer.android.com/studio
- 或者单独下载 Android SDK
- 脚本会自动搜索以下位置：
  - `%LOCALAPPDATA%\Android\Sdk`
  - 脚本同级目录的 `android-sdk` 文件夹
  - `C:\Android\Sdk`、`D:\Android\Sdk`、`E:\Android\Sdk`

> **提示**：如果你没有 Android SDK，可以把 SDK 文件夹放在本工具同级目录下，命名为 `android-sdk`，脚本会自动找到。

---

## 三、使用步骤

### 第 1 步：准备"打包版文件"

你需要从 Roche 官方获取"打包版文件"文件夹（里面包含 `dist/index.html` 等 Web 构建产物）。

### 第 2 步：放入打包版文件

把"打包版文件"文件夹放到**本工具的根目录下**，文件夹名保持为 `需要打包版文件`。

目录结构应该如下：

```
roche-apk-打包工具/
├── build-apk.cmd              ← 一键打包入口（双击运行）
├── build-apk.ps1              ← 打包脚本（PowerShell）
├── 打包指南.md                ← 本文件
├── keepalive-plugin/          ← 保活插件源文件（含静音音频）
│   ├── plugin.js
│   ├── manifest.json
│   └── silence.wav            ← 20秒静音音频（用于保活）
├── templates/                 ← Android 原生模板
│   └── android-background-audio/
│       └── BackgroundAudioService.java.template
└── 需要打包版文件/             ← 你放进去的文件夹
    ├── dist/
    │   └── index.html
    ├── capacitor.config.json
    └── package.json
```

### 第 3 步：双击运行 `build-apk.cmd`

脚本会自动完成以下所有工作：
1. 检查环境（Node.js、JDK、Android SDK）
2. 检查项目文件完整性
3. 复制 `silence.wav` 到 Android 资源目录
4. 安装 Node.js 依赖（含蓝牙插件）
5. 同步 Capacitor 原生插件
6. 执行准备脚本（生成图标、配置权限、安装模板）
7. 覆盖 BackgroundAudioService.java（确保保活修复生效）
8. 配置 Gradle 构建环境（中文路径修复）
9. 构建 APK
10. 输出最终 APK 文件

### 第 4 步：获取 APK

打包完成后，APK 文件会出现在：
- `roche-apk-打包工具\roche-app-debug.apk`

同时脚本会自动打开所在文件夹。

---

## 四、安装保活插件

APK 安装后，还需要在 Roche 中安装 Keep Alive 插件才能启用原生保活。

### 插件地址

在 Roche 的插件管理中，添加以下 GitHub 链接：

```
https://raw.githubusercontent.com/luyi90720-sys/keepalive/main/plugin.js
```

### 插件功能

- **原生前台服务保活**（需要 APK）
- **20秒静音音频循环**（内置在 APK 中，无需网络）
- **自动检测环境**：APK 中用原生服务，浏览器中用 Web Audio
- **诊断面板**：可查看 Capacitor、插件、桥接状态

### 使用方法

1. 在 Roche 中打开 Keep Alive 插件
2. 查看底部 **Diagnostics** 面板：
   - `window.Capacitor` = EXISTS ✓
   - `isNativePlatform` = true ✓
   - `Plugins.BackgroundAudio` = EXISTS ✓
   - `bridge.__ready` = true ✓
3. 如果 Diagnostics 全部显示绿色，点击 **Start** 按钮
4. 通知栏会出现 "Roche Keep Alive"，保活生效

> **注意**：如果 Diagnostics 显示红色，说明 APK 版本过旧，请重新打包安装最新 APK。

---

## 五、常见问题

### Q1：提示"未找到 JDK 21+" 或 "Cannot find a Java installation matching: {languageVersion=21}"
- 必须安装 **JDK 21+**（JDK 17 不够！capacitor-filesystem 8.1.2 要求 JDK 21）
- 下载 Eclipse Temurin 21：https://adoptium.net/temurin/releases/?version=21
- 确保安装了完整 JDK（带 jlink.exe），不是 JRE
- 安装后重启电脑，确保环境变量生效

### Q2：提示"未找到 Android SDK"
- 安装 Android Studio，通过 SDK Manager 下载 SDK
- 或把 SDK 文件夹放在本工具同级目录下，命名为 `android-sdk`

### Q3：构建失败，提示中文路径错误
- 脚本已自动配置 `android.overridePathCheck=true`
- 确保整个路径中没有特殊字符（中文可以，但避免符号）

### Q4：Keep Alive 插件显示 "Web 保活"而不是 "Native Service"
- 说明 APK 版本过旧，`BackgroundAudio` 插件未注册
- 请重新打包安装最新 APK
- 新版 APK 中 `registerPlugin()` 已在 `super.onCreate()` 之前调用

### Q5：保活还是不生效？
- 检查 Diagnostics 面板各项是否全绿
- 点击 **Retry Init** 按钮重新初始化桥接
- 确认手机系统没有限制后台运行（小米/华为等品牌需手动允许自启动）
- 确认通知权限已开启（前台服务需要通知）

### Q6：蓝牙连上玩具但发送失败
- 这是已修复的已知问题，请重新打包最新 APK
- 蓝牙插件已配置为发送 hex 字符串格式

---

## 六、文件说明

| 文件 | 作用 |
|------|------|
| `build-apk.cmd` | 一键打包入口，双击运行 |
| `build-apk.ps1` | 实际打包脚本（PowerShell） |
| `keepalive-plugin/silence.wav` | 20秒静音音频，复制到 APK 的 res/raw/ |
| `keepalive-plugin/plugin.js` | 保活插件源码（参考，实际从 GitHub 安装） |
| `templates/.../BackgroundAudioService.java.template` | Android 前台服务模板 |

---

## 七、技术细节（给开发者）

### 保活链路

```
用户点击 Start
    ↓
keepalive plugin.js (v3.1)
    → waitForNativeAudio(8000ms) 等待桥接就绪
    → window.nativeAudioBridge.startKeepAlive()
    ↓
native-audio-bridge.js (v2.0) — 内置在 APK 的 dist/ 中
    → Capacitor.Plugins.BackgroundAudio.replaceQueue({
        tracks: [{ url: 'android.resource://com.roche.app/raw/silence' }],
        mode: 'loop', autoplay: true
      })
    ↓
BackgroundAudioPlugin.java — Capacitor 插件
    → BackgroundAudioService.replaceQueue()
    ↓
BackgroundAudioService.java — Android 前台服务
    → 解析 android.resource:// URL
    → openRawResourceFd() → MediaPlayer
    → startForeground() 显示通知
    → 循环播放 20s 静音音频 → 系统保活
```

### 关键修复点

1. **registerPlugin 顺序**：必须在 `super.onCreate()` **之前**调用
2. **静音音频本地化**：使用 `android.resource://` 而非远程 URL，无需 VPN
3. **桥接初始化**：3 路初始化（直接 + capacitorReady 事件 + DOMContentLoaded）
4. **waitForNativeAudio**：插件等待桥接就绪，避免时序问题
