# 贡献提交指南 —— 三组 PR 最终交付版（2026-08-24 定稿）

> **分支**：`contrib/clean` @ `d304663`（已推送 `yechen1844/ai-virtual-phone`）
> **基线**：官方 main `9f22809`；18 个贡献文件在官方基线上 tsc 通过（仅剩官方自身 2 条 Buffer 噪音）
> **合并顺序硬性要求：#146 → #147 → #148**

---

## 一、三组清单（勾选严格按此，不多不少）

| 组 | 主题 | 六个文件 |
|----|------|---------|
| **A·先交** | 引擎核心修复+日志分流+会话级流式 | `lib/api-helpers.ts` · `lib/chat-engine.ts` · `lib/chat-offline-storage.ts` · `lib/chat-storage.ts` · `lib/group-chat-engine.ts` · `lib/settings-types.ts` |
| **B·次交** | UI层+工坊接入 | `components/chat/chat-room.tsx` · `components/chat/chat-settings-panel.tsx` · `components/phone-qa-app.tsx` · `lib/qa-agent-engine.ts` · `styles/chat.css` · `styles/qa.css` |
| **C·后交** | 配套功能 | `components/chat/sticker-search-suggest.tsx` · `components/chat/user-profile-panel.tsx` · `components/settings/preset-manager.tsx` · `lib/llm-prompt-assembler.ts` · `lib/memory-summarizer.ts` · `lib/offline-prompt-builder.ts` |

---

## 二、PR 标题与说明文案（可直接粘贴）

### 组 A 标题
```
A: fix(chat) 引擎核心修复+日志分流+会话级流式+善后完善（基于最新 main 重切版）
```

### 组 A 说明
```markdown
本版为按上轮评审意见完成的最终版，逐条对应：

【两项阻断的解决】
- 已基于最新 main 重切：官方 skipTimestampStrip 流式改动与本组改造按评审给出的
  合并式共存（readSseStream 四参调用 + rawOutput 三元选择）。
- chat-storage 云同步修复原样保留：CHAT_RESPONSE_BATCH_REPLACED_EVENT 导出与派发、
  replaceResponseBatchWithParts 两处及 replaceGroupResponseRound 一处 cloudSync 身份均未触碰。

【上轮三条非阻断意见的落实】
1. 群聊版摘要补提已瘦身：对齐单聊版「系统提示 + 最后一条 user 消息 + 本次输出」，
   不再重发完整 llmMessages。
2. pushChatMessage 增量缓存行为对等性核对结论：
   - 会话排序 ✓：就地更新缓存对象的 updatedAt/lastMessageId/预览，列表读取同一缓存；
   - 预览刷新 ✓：UI 通知由 CHAT_MESSAGE_PUSHED_EVENT 承担且无条件派发
     （saveChatSessions 本身从不派发事件，无遗漏）；
   - 存储镜像 ✓：dbPutSessions 为 Dexie bulkPut 主键级 upsert，单条写等价；
   - 另保留缓存未命中时回退 loadChatSessions+saveChatSessions 全量路径的兜底分支。
3. 流式分支已传 debugSessionId：sendLLMStreamRequest 新增该选项并透传至插件请求/
   streamChunk 事件/响应处理，同时补发提示词快照（requestKind=completion）。

【主动申报：一项有意保留的功能】
- 线下回复缺失 <summary> 时的自动补提（单聊 generateOfflineChatCompletion 与
  群聊 generateGroupOfflineChatCompletion 各一处）为本组功能设计的一部分，
  群聊版已同步瘦身。如认为不宜随本组合入请指出，作者即行剥离。

其余内容与前版一致：续写误判修复（看原始 history 末尾）+ isToolFlowHistoryMessage 守卫、
apiLogChannelFor 显式分流、流式思维链落日志、stripPresetTexts 字面量删除、
extractThinkingTag 思维链提取、官方 memory 计数/总结触发保持不变。

合并顺序：本 PR 先合（B/C 两组对本组存在类型级依赖）。
```

### 组 B 标题
```
B: feat(chat/工坊) UI层+工坊接入+善后完善（基于最新 main 重切版）
```

### 组 B 说明
```markdown
【上轮四点意见的落实】
1. qa.css 中被删的「消息级懒渲染」两行注释已恢复（官方 #135 原文）。
2. 线下「重试」路径的流式预览已对齐首次发送路径的 rAF 合并限频
   （每帧最多一次全文解析+setState，复用 offlineStreamFrameRef 与既有卸载清理）。
3. 两处未申报改动现予申报（确系 D 系列 #133 内容混入，归属暂放本组，如需挪回 D 请指示）：
   - 表情包搜索联想：发送后清空输入框；
   - 「触发回复」按钮：输入框有文字时改为「先发送再触发」。
4. ctx-menu 美化已整体移除：chat.css 中 ctx-menu 区域已逐字回退为官方实现，
   本组不再携带任何 ctx-menu 观感改动，无需拆分 PR。

【配套关系申报】
- sticker-search-suggest.tsx 注释所述「发送后由父组件清空输入」，其清空行为实现在
  本组 chat-room.tsx。两者成对，请勿单独回滚其一。

【官方时序补充：ffcc8e3 工坊会话菜单】
- main 已合入 ffcc8e3（采纳自 #151），本版已在其之上叠加：图标行双方并存
  （FileCode2 + MoreVertical/Pin/PinOff）、apiLogOpen 与 renameTarget 两套 state 并肩、
  qa-rename 弹窗样式与逻辑完整。

【依赖】硬依赖组 A（extractThinkingTag、ChatSession.streamOnline/streamOffline、
onStreamDelta 回调、thinking/thinkingTag 字段）。请在组 A 合入后跟进本组。
```

### 组 C 标题
```
C: feat(chat) 配套功能+善后完善（基于最新 main 重切版）
```

### 组 C 说明
```markdown
【本轮更新】
- regexFromString 编译缓存补充注释立牌：缓存返回共享 RegExp 实例，带 g/y 标志的
  实例携带 lastIndex 状态，自行驱动匹配的调用方需在使用前 reset 或复制实例
  （落实上轮评审的可选建议）。

【与前版一致的申报】
- 预设管理器新增五字段（thinking_tag / online_thinking_tag / 思维链开关×2 /
  strip_texts）与引擎侧（组 A）读取一一对应，无悬空配置；
- simpleLLMCall 新增 label 选项；DebugInfo.reasoning 展示；
- 记忆总结调用带「记忆总结·角色名」来源标签，配合 hiddenSources 反向集合来源过滤闭环；
- 正则编译缓存解决显示层每消息每帧重复编译的真实痛点。

【配套关系】sticker-search-suggest 的「发送后清空输入」行为实现在组 B chat-room，
已在组 B 说明中申报，两者成对。

【顺序】类型级依赖组 A，按 A → B → C 最后合入。
```

---

## 三、工坊提交操作步骤

1. 打开工坊 → 贡献入口，fork 填 `yechen1844/ai-virtual-phone`，分支填 `contrib/clean`
2. **第一次提交**：只勾选组 A 的 6 个文件 → 标题/说明复制上文「组 A」→ 提交（新编号，如 #150）
3. **等 A 合入官方后**：第二次提交勾选组 B 的 6 个文件 → 用「组 B」文案
4. **等 B 合入后**：第三次提交勾选组 C 的 6 个文件 → 用「组 C」文案
5. 若期间官方 main 又前进导致冲突提醒：先同步最新 main、在本地重做对齐后再提交（回来找我操作）

---

## 四、红线备忘（每次提交前过一遍）

- 只用 `contrib/clean` 提交；`main` 含全套私有功能，永远不作为提交来源
- 复杂记忆系统任何文件/引用不得出现在勾选清单（含 `ComplexMemoryToggle`）
- 勾选按三组各 6 个文件的清单来，不多不少
- 不携带任何私有内容：日志耗时 durationMs、预设多选/批量、双语投喂 translationFeedMode、Roche 导入 importChatMessages、OpenCode 代理、阅读摘要 readingSummary、记忆优化本地增强——这些已全部剥离，勿再加回
- `lib/stream-preview.ts` **不在清单、也不需要交**：它是官方 main 已有文件（#141 基础件合入），本地与官方逐字节一致；chat-room.tsx 对它的 import 在官方基线直接可解析。同理，凡"清单外但 import 报缺失"的疑问，先查官方 main 是否已有该文件（`git cat-file -e upstream/main:路径`），有则不用交

## 五、分支状态记录

`contrib/clean` @ `d304663` 相对 main 的全部差异 = 净贡献（18 文件，+1463/−262）。
净化历史：aac5659 → 2b5cacc → baf2e1a → cf9e3df → d304663（终审：剥耗时/投喂/导入/OpenCode，恢复官方总结钩子，清编辑噪音）。
验证：官方基线 tsc 通过；净 diff 无任何私有标记。
